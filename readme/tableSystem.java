import java.*;

public static void main(String []args)
{
	
	public int floor = 1;
	public final int enemyRate = .5;
	public int enemies = ceiling(floor * enemyRate);

	class TableSystem
	{
		for (floor; floor > enemies; floor++)
		{
			string enemyList[enemies];			
			int enemy = math.random();
			if (enemy < .3) 
			{
				enemyList[--floor] = skeleton;
				//ADD MAKE skeleton.mob
			}

			else if (enemy > .3 && enemy < .6)
			{
				enemyList[--floor] = slime;
				//ADD MAKE slime.mob
			}

			else if (enemy > .6)
			{
				enemyList[--floor] = bat;
				//ADD MAKE bat.mob
			}			
}
